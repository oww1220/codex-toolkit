interface:
  display_name: "Subagent Orchestration"
  short_description: "계획, 분할, 검증을 전역 서브에이전트로 운영"
  default_prompt: "Use $subagent-orchestration to spawn only the read-only analysis or review roles the task needs. The main agent performs implementation and final verification."
