---
name: {{NAME}}
description: {{DESCRIPTION}}{{ALLOWED_TOOLS}}
---

# {{TITLE}}

> ⚙️ scaffold가 생성한 골격이다. `{{...}}`와 `[채울 것]`을 채우고, 안 쓰는 절은 지운다. 작성 규칙은 skill-enhancer 의 recipes·methodology 레퍼런스를 따른다.

## 무엇을 / 언제 (한 줄)
[이 스킬이 하는 일 한 문장. description과 일관되게.]

<!-- IF:multimode -->
## Step 0 — 무엇을 만들지 고른다
| 모드 | 시작점 | 언제 |
|---|---|---|
| **[모드A]** (기본) | [템플릿/경로] | [조건]. **기본값** |
| **[모드B]** | [템플릿/경로] | [조건] |

판단: [한 줄 휴리스틱]. **애매하면 request_user_input으로 묻는다 — 임의로 고르지 말 것.**
<!-- ENDIF:multimode -->

## 제약 (MUST DO / MUST NOT DO) — 스텝 위에 둔다
- ✅ [반드시 하는 것]
- ⛔ [절대 안 하는 것]
- ⛔ 출력은 cwd 상대 경로 + `mkdir -p` 가드로만. 채팅 덤프·스킬 설치 디렉터리 쓰기 금지.

## 핵심 워크플로우
1. [단계 1 — 판단/입력 파악]
2. [단계 2 — 작업]
3. [검증 — 가능하면 `scripts/`의 스크립트가 non-zero로 종료하는 게이트. prose "수동 확인" 금지]
4. [출력 — 산출 경로 명시]

<!-- IF:orchestrator -->
## Pipeline Context
| 항목 | 내용 |
|------|------|
| **파이프라인 위치** | [위치] |
| **입력 소스** | [입력] |
| **출력 대상** | [출력] |
| **호출 스킬/에이전트** | [목록] |

- 디스패치: 사용 가능한 Codex tool 또는 subagent 도구를 명시한다.
- 서브에이전트엔 (컨텍스트 공유 안 함) 핵심 산출물 경로 + 규칙 + 산출 경로 + 금지목록을 매번 명시.
- 캡: max_turns ≤ 15, 병렬 ≤ 3~4, 결과는 한 줄(`DONE:<id>`/`FAIL:<id>:<reason>`).
<!-- ENDIF:orchestrator -->

<!-- IF:references -->
## 참조 자료 (라우팅 테이블)
| Topic | Reference | Load When |
|-------|-----------|-----------|
| [주제] | references/[file].md | [이 조건일 때 읽는다] |

> 각 reference 파일은 `> **로드 시점**: <같은 조건>`으로 시작해 트리거를 메아리친다.
<!-- ENDIF:references -->

## 출력 위치 (필수)
- ✅ 필수: `output/{slug}/` 등 cwd 상대 + `mkdir -p` 가드
- ❌ 금지: 채팅 코드 덤프 / 스킬 설치 디렉터리 안 임시 산출물 / 홈 절대경로

> **단일 진실원**: 템플릿/토큰/스키마 값은 정의된 소스에서 그대로 바인딩하고, 자리표시자만 채운다 — 구조·CSS·스키마는 변경 금지.
